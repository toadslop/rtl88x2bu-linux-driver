// SPDX-License-Identifier: GPL-2.0
/* Kernel accessors for rust/rtw_recv_sta*.rs (W3-85). */
#include <drv_types.h>

#if defined(CONFIG_RUST_RECV_STA) && !defined(HOST_RECV_STA_TEST)

/* Populated in W3-85 PR2 (validate_hdr + kernel object swap). */

#endif /* CONFIG_RUST_RECV_STA && !HOST_RECV_STA_TEST */
