# Example L1 allowlist for check-symbols.sh (T1).
#
# Intentional symbol drops/renames should be rare in Phase 1 parity ports.
# Copy this file per swap and uncomment/edit rules when the PR documents why.
#
# Working sample with real rules (used by rust-check-symbols-selftest):
#   docs/rust-migration/scripts/fixtures/omac1_vs_aes_ctr.allow
#
# drop legacy_helper_only_used_inside_c_file
# rename old_export_name new_export_name
