---
title: "[Epic] Wave 6 — RfL module! entry"
labels: [rust-migration, phase-1, wave-6]
type: epic
id: E08
blocked_by: [E07]
---

## Goal

Move `module_init` / `module_exit` (`rtw_drv_entry` / `rtw_drv_halt`) to Rust-for-Linux `module!` without behavior change; update DKMS/docs for Rust-enabled kernels.

## Children

**Not filed yet** (expect a few small issues at end of Phase 1).
